# mForceLibrary — Project Configuration & Infrastructure Inventory

> **Last Updated:** 2026-02-25  
> **Protocol:** `fluXiveOS_development-protocol.md` §3  
> **Maintained by:** mForce Agent — update whenever a new resource is provisioned.

---

## A. Google Cloud Platform (GCP) Resources

| Resource Type | Name/ID | Region | Purpose |
| :--- | :--- | :--- | :--- |
| GCP Project | `mforcelibrary` (Project #: `739704983321`) | global | Root project for all Library infrastructure |
| Cloud Build | `cloudbuild.yaml` (auto-triggered) | `global` | CI/CD pipeline for Docker build + Cloud Run deploy |
| Artifact Registry | `fx-library` | `us-east1` | Docker image repository for mforcelibrary + fxagent-oauth |
| Cloud Run Service | `mforcelibrary` | `us-east1` | Hosts the VitePress static site behind nginx |
| Cloud Storage Bucket | `gs://mforcelibrary_cloudbuild` | auto | Staging bucket for Cloud Build source tarballs |

### IAM Bindings (Service Account: `739704983321-compute@developer.gserviceaccount.com`)
| Role | Purpose |
| :--- | :--- |
| `roles/artifactregistry.writer` | Push Docker images during build |
| `roles/cloudbuild.builds.builder` | Execute Cloud Build steps |
| `roles/logging.logWriter` | Write build logs to Cloud Logging |
| `roles/run.admin` | Deploy to Cloud Run |
| `roles/iam.serviceAccountUser` | Act-as service account for Cloud Run deployment |
| `roles/storage.objectAdmin` | Read/write build source tarballs |

---

## B. Local Resources

| Resource | Details |
| :--- | :--- |
| Node.js | v20 (via Docker `node:20-alpine`) |
| Package Manager | npm v11 (`packageManager: npm@11.6.2`) |
| Framework | VitePress `^1.3.4` |
| Dev Server Port | `5173` (via `npm run docs:dev`) |
| gcloud Config Override | `.gcloud-config/` (local workaround for sandbox permission issues) |

---

## C. Mappings & Network Topology

### Domain
| Domain | Service |
| :--- | :--- |
| `library.fluxive.ai` | → Cloud Run `mforcelibrary` (us-east1) |

### Port Mappings (Local Development)
| Port | Service |
| :--- | :--- |
| `5173` | VitePress dev server (`npm run docs:dev` → `sh mforce.sh test`) |
| `8080` | nginx inside Docker container (production) |

### External Integrations
| Service | URL | Purpose |
| :--- | :--- | :--- |
| Fluxive Launchpad | `https://machineforce.fluxive.ai/launch.js` | Authentication hypervisor (SRI enforced) |
| Fluxive.ai | `https://fluxive.ai` | Logo link + copyright target |
| Google Fonts | `fonts.googleapis.com` | Inter, Exo 2, Orbitron, Share Tech Mono, JetBrains Mono |

### Environment Variables (Keys Only)
| Key | Used In | Purpose |
| :--- | :--- | :--- |
| `GCP_PROJECT` | `mForce.sh` | Set to `mforcelibrary` |
| `CLOUDSDK_CONFIG` | `mForce.sh` (local override) | Points to `.gcloud-config/` |
| `fluxive-theme` | `localStorage` (browser) | Persists solar/lunar toggle state |

---

## D. Key File Paths

| File | Purpose |
| :--- | :--- |
| `docs/.vitepress/config.mts` | VitePress site config, head scripts, nav/sidebar |
| `docs/.vitepress/theme/index.ts` | Theme layout, Launchpad mount, auth event listener |
| `docs/.vitepress/theme/style.css` | Global CSS design system, Solar/Lunar variables |
| `docs/.vitepress/theme/components/Perimeter.vue` | Landing page (unauthenticated state) |
| `docs/.vitepress/theme/components/SolarToggle.vue` | Sun/Moon theme FAB |
| `docs/.vitepress/theme/components/StandardTicker.vue` | Footer ticker bar |
| `docs/.vitepress/theme/components/NeuralCurator.vue` | Authenticated dashboard search interface |
| `docs/public/` | Static assets (logo.svg, google-badge.png, config.json) |
| `cloudbuild.yaml` | GCP Cloud Build pipeline definition |
| `Dockerfile` | Multi-stage: node:20-alpine builder → nginx:alpine runner |
| `nginx.conf` | nginx config for Cloud Run (port 8080) |
| `mForce.sh` | Unified project management script |

---

*Auto-generated by VisionSync audit — 2026-02-25*
